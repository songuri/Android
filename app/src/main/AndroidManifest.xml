<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.songuriproject">
    <!-- 개인정보와 관련되지 않은 것들은 설정에 표기 되지 않음 -->
    <!-- 수동으로 사용자가 설정가서 활성화 시키지 않게 하는게 좋음 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".inflearn.thread.AsyncTaskActivity"></activity>
        <activity android:name=".inflearn.thread.HandlerActivity" />
        <activity android:name=".inflearn.thread.ThreadActivity" />
        <activity android:name=".inflearn.InflearnMainActivity" />
        <activity android:name=".inflearn.Permission.PermissionActivity" />
        <activity android:name=".testpage.AnimationTestActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>